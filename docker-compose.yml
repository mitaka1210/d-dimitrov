version: '1.0'

services:
  app:
    build: .
    container_name: eng.d-dimitrov.eu
    volumes:
      - .:/app
    restart: always
    command: npm start
networks:
  default:
    external:
      name: docker_network
