version: '3.8'

services:
  app:
    build: .
    container_name: d-dimitrov
    volumes:
      - .:/app
    command: npm start
    restart: always
networks:
  default:
    external:
      name: docker_network
