import React from "react";

export const metadata = {
  title: "About Me - eng.Dimitrov",
};

export default function AboutLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return <>{children}</>;
}
